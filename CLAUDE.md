# Проект: Честный знак × МойСклад (COSMEX)

Интеграция маркировки «Честный знак» с МойСклад для COSMEX (Россия).
Юрлицо: **ИП РОДИОН НАМСЕПОВИЧ / COSMEX Россия, ИНН 421715237155**.
Две точки продаж (Центральный, «1. Фантазия»/ТЦ Калина) — **одно юрлицо, один ИНН**
(перемещения между ними в ЧЗ не отражаются).

## ⏰ Окно работы подписи (ВАЖНО — операционное ограничение)

УКЭП (Rutoken) и агент подписи (`deploy/office-signer/agent.ps1`) живут на **кассовом ПК Ксении**.
Этот ПК **включён ежедневно с 10:00 до 21:00 по Новосибирску (UTC+7)** = **03:00–14:00 UTC**.
На ночь ПК **выключается** → агент офлайн → подпись недоступна.

Поэтому любые операции, которым нужна подпись УКЭП, выполнимы **только в это окно**:
- авторизация СУЗ (challenge-sign), нанесение, ввод в оборот;
- True API, где нужен свежий токен (`cises/info`, создание документов).

Вне окна получишь `504 Офисный подписант не ответил вовремя`. Планируй боевые запросы
и cron-сверки на 10:00–21:00 НСК. **Чтение Telegram-группы подписи НЕ требует** — доступно всегда.

## Архитектура подписи (pull-модель)

Гвард (Hetzner, `https://guard.89-167-54-69.sslip.io`, `/opt/cosmex-chestny-znak`) кладёт
запрос в очередь (`SignQueue`); `agent.ps1` на кассовом ПК опрашивает `/sign/poll`, подписывает
через КриптоПро csptest по отпечатку сертификата и возвращает подпись на `/sign/result`.
С сервера «пингануть» агента нельзя — только дождаться следующего опроса.

## Ключевые факты

- **СУЗ (авторизация):** clientToken для suzgrid = **токен True API (markirovka)**, тот же, что
  в `cis-info`. Свой `suzgrid/auth/simpleSignIn` для участника-ОМС даёт **4035 «сервис-провайдер
  не найден»** (этот путь для операторов, не для нас). `SuzClient` берёт токен через
  `trueApi.ensureToken()`. OMS ID + «идентификатор соединения» — `CRPT_OMS_ID`/`CRPT_OMS_TOKEN` в `.env`.
  СУЗ geo-блока НЕТ (с non-RU IP отдаёт 400/1090, не 451) — RU-прокси не нужен.
  После фикса токена `suz-ping` даёт **1090** на `657fed11` (правильный omsId, дошло до проверки
  реквизитов) и **1110 «не указано значение идентификатора СУЗ»** на `a0c2797c` (это connectionId,
  не omsId). Значения НЕ перепутаны, код верный. **1090 = ОМС не активирован/не признан в ЛК СУЗ**
  (проверить: активна ли станция ОМС и соединение, привязан ли ИНН 421715237155 и товарные группы,
  тем ли сертификатом). Это блокер на стороне ЛК, не кода.
- **Статусы КМ (cis-info) — как читать владельца:** INTRODUCED + ownerInn нашего ИНН = наши;
  ownerInn поставщика (напр. ПРОМЭФ 7814581477, ПЛАНЕТ НЭЙЛЗ 7743568999) = товар у нас, но приёмка
  КИЗ по ЭДО не проведена → продавать/выводить нельзя, надо подписать входящий УПД. «Нет статуса»
  часто = битый серийник (умные кавычки »''"/кириллица из-за копирования) → перечитать сканером.
- **Telegram:** бот-админ в группе «‼️🌿ВАЖНОЕ COSMEX🌿‼️» (супергруппа `-1003954509880`).
  Вебхук на гвард с `secret_token=WEBHOOK_SECRET`. Чтение инбокса:
  `GET /webhook/telegram/<WEBHOOK_SECRET>/inbox?after=<id>`. Прогресс — `scratchpad/tg-last-id.txt`.
- **ЭДО Лайт / Диадок-интеграция — решено НЕ подключать** (28.08.2026). Права на КМ переходят
  на нас при подписании входящего УПД в Диадоке (веб, вручную) — этого достаточно. Продажа/отгрузка
  ловят код сканированием (касса / «Отгрузка»), а не из приёмки. Интеграция дала бы только
  автосоздание приёмок в МойСкладе (удобство при большом потоке накладных) — при их объёме не окупается.
- **Ветка разработки:** `claude/honest-sign-cosmex-integration-nukn89`. Гвард авто-обновляется по /health.
- **Node ≥22.18**, zero-dependency TypeScript (нативный type-stripping), без сторонних зависимостей.
- **Владелец нетехнический**, отвечает по-русски. У него есть SSH к гварду и RU-серверу (свой ключ).

## Журнал 01.09.2026 (крем-остатки, интеграции)

- **Гвард — новые маршруты** (ветка `claude/honest-sign-cosmex-integration-nukn89`, задеплоено):
  `GET /webhook/telegram/<SECRET>/file?file_id=…` — скачать вложение TG (токен бота на сервере);
  `POST /webhook/telegram/<SECRET>/send?text=…` — sendMessage; с `?filename=&caption=` и бинарным
  телом — sendDocument. Так бот читает скрины из группы и отвечает/шлёт файлы Ксении.
- **Крем (GTIN 04620172705532, 17 КМ) — РАЗОБРАНО, шли не тем путём:** крем **закупной** (поставщик,
  произведён 09.2025, завезён 04.2026). Поставщик прислал 17 КМ **по ЭДО**; на флаконах физических
  кодов нет. COSMEX по ошибке **сам заказал** свои 17 КМ (СУЗ, 28.08) и сделал нанесение → коды
  «Нанесен», ввод в оборот ЧЗ не даёт («для ТГ Косметика функционала маркировки остатков НЕТ» —
  офиц. ответ ЧЗ). ПРАВИЛЬНО для покупателя: **приёмка входящего УПД поставщика в Диадоке**, а не
  наша маркировка/ввод. Наши 17 КМ от 28.08 — **ошибка, не используем** (можно списать). Открытый
  вопрос к поставщику: почему товар без физических DataMatrix (тикет ЧЗ 7da3fea2…, ответ дан).
  Этикетки/коды в scratchpad (`suz-krem-codes.txt`, `krem75-*.pdf`) — от ошибочного набора, не нужны.
- **Сканер MINDEO MD6600-HD** (серийник KK01095727, прошивка MD6600_APP_V2.2.4) не передавал GS →
  код выходил слитно (`…serial91EE1292…`), касса писала «Некорректный формат», веб-ЧЗ не принимал.
  **РЕШЕНО (02.09):** касса = МойСклад; настроен по офиц. штрихкодам МойСклад (сброс→суффикс F8→HID→
  «передача скрытых символов» 3 штрихкода→выход). Лист собран в `scratchpad/MINDEO-MD6600-peredacha-GS.pdf`
  (источник: support.moysklad.ru/…/сканер_штрихкодов_mindeo_md6600_hd, картинки 01_factory_reset…07_exit).
  После — на кассе чек с маркир. товаром «Ⓜ Без ошибок». Продажа маркированного и веб-нанесение
  разблокированы.
- **СУЗ/ОМС (скрин Устройства):** OMS ID **657fed11-d356-4160-b1e6-60afa1b3b22c** (совпадает с env),
  два соединения: «Cosmex integration» `a0c2797c-44c8-48de-abdf-42d9244da350` (= наш CRPT_OMS_TOKEN)
  и «ИП Но Родион Намсепович» `7445f08e-cf94-48e1-8166-3bdc16495dfb`. ТОКЕН/срок — пусто. Проверить
  «Разрешения»/товарные группы у соединения — вероятная причина 1090.
- **Касса по алмадезам/мед.крему готова** (разрешительный режим настроен — слова Ксении).
- **Диадок:** входящих УПД на подписание нет → поставщики (ПРОМЭФ, ПЛАНЕТ НЭЙЛЗ) ещё не прислали
  УПД по июньским поставкам (40 кодов). Нужен запрос поставщикам, не работа в Диадоке.
- **Маркетплейсы** (`cosmeeex`, `mp.cosmex.ru` = тот же Hetzner 89.167.54.69, `/opt/seo-bot`,
  nginx→127.0.0.1:8501): коды WB/Ozon/Yandex + черновик отгрузки при приходе заказа.
  Флаг `MARKING_CODES_ENABLED` **выключен** (в `.env` его нет) — коды на МП не передаются, dormant.
- **Гвард-кнопка боевого dry-run (02.09, задеплоено):** `POST /webhook/telegram/<SECRET>/ozon-dryrun[?cab=]`
  — на сервере делает `git pull` + запуск `dryrun_ozon_marking.py` (ничего не шлёт в Ozon, не пишет
  в МС, только логирует). Путь под `/webhook/telegram/`, т.к. **nginx проксирует на гвард только этот
  префикс** (произвольный `/ops/` режет 401). Команда жёстко зашита, `cab` валидируется.
- **Dry-run Ozon — ПРОВЕРЕН (02.09):** прогнан помощником в изолир. копии master (`/root/ozon_dryrun`,
  symlink на *_settings.json+cache.db, `.env` из `/opt/seo-bot`). Механика работает: 2 заказа
  «Готов, Собран» отсеялись штатно (Ozon `awaiting_deliver` ≠ нужного `awaiting_packaging`) → `[DRY]`-строк
  нет, т.к. подходящего заказа сейчас нет. Чтобы увидеть коды — дождаться живого заказа Ozon
  «Ожидает сборки» с маркир. товаром, отсканировать DataMatrix в «Отгрузку» МС, повторить.
- **Ветки маркетплейсов сведены на master (02.09, `b0e42b4`) — ЗАДЕПЛОЕНО:** прод сидел на
  `feature/chestny-znak-marking` и разошёлся с master (в master — `196939b` безопасный dry-run + `775fc22`
  скрипт; в проде — `2acf665` возвраты/Halyk/WB). Слито в master (чисто, `ozon_sync.py` не конфликтует).
  Прод `/opt/seo-bot` **на master @ b0e42b4**, `seo_bot`/`seo_web` перезапущены (18:34 UTC), проверки
  зелёные (`:8501`→307→/login — это норм, редирект неавторизованного). Юниты: `seo_bot.service`,
  `seo_web.service`. `cosmex-app-update` держит прод на master (берёт текущую ветку). Точка отката —
  `feature/chestny-znak-marking @ 2acf665`. Флаг `MARKING_CODES_ENABLED` в `.env` НЕТ — включать только
  после верных `[DRY]`-строк на живом заказе.
  ⚠️ Прод-приложение НЕ авто-обновляется как гвард (HTTPS-remote с read-only PAT, `git pull` тянет
  текущую ветку) — можно позже настроить авто-деплой master (deploy-ключ + таймер), по желанию.
  ⚠️ Замечено при деплое: `fetch-probe-*.service (failed)` с `git fetch … feature/chestny-znak-marking`
  и `cosmex-app-update` в `activating` — проверить, не хардкожена ли где старая ветка (иначе безвредно).

## Runtime CLI (гвард)

- `node src/cli.ts cis-info <файл-кодов>` — статус КМ в ГИС МТ (нужна подпись).
- `node src/cli.ts suz-ping [группа]` — проверка связи СУЗ (нужна подпись).
- прочее — `node src/cli.ts` без аргументов.
