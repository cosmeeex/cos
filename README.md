# cosmex.ru — SEO-аудит и исправления

Аудит продакшена **cosmex.ru** от 18.08.2026: технические ошибки, готовые патчи и точки роста трафика.

> **Про этот репозиторий.** На момент работы `cosmeeex/cos` был пуст — ни одного коммита, ни одной ветки. Исходников сайта здесь нет, поэтому правки в код приложения внести было негде. Всё, что можно было сделать без доступа к исходникам, сделано: сайт разобран через прямые HTTP-запросы с UA реальных краулеров, каждая находка доказана и воспроизводима, а исправления оформлены готовыми к применению патчами. Как только исходники появятся в репозитории — патчи из `fixes/` переносятся в код напрямую.

## Что внутри

| Файл | О чём |
|---|---|
| [`docs/seo-audit.md`](docs/seo-audit.md) | Полный аудит: 12 находок с доказательствами |
| [`docs/growth-plan.md`](docs/growth-plan.md) | 9 точек роста трафика и очерёдность работ |
| [`fixes/nginx/cosmex-cache.conf`](fixes/nginx/cosmex-cache.conf) | Правки кэша: P0-1, P1-3, P1-4 |
| [`fixes/next/pagination-404.md`](fixes/next/pagination-404.md) | P0-2: бесконечная пагинация |
| [`fixes/next/next.config.md`](fixes/next/next.config.md) | P0-1, запасной вариант на стороне приложения |
| [`fixes/next/internal-links.md`](fixes/next/internal-links.md) | P1-5, P1-6, P2-8: перелинковка |
| [`fixes/next/product-schema.md`](fixes/next/product-schema.md) | P2-10: `shippingDetails`, `hasMerchantReturnPolicy`, отзывы |
| [`fixes/next/images.md`](fixes/next/images.md) | P1-3: LCP-картинка главной |
| [`fixes/next/sitemap.md`](fixes/next/sitemap.md) | P2-9 и прочее по sitemap.xml |
| [`tools/seo-check.sh`](tools/seo-check.sh) | Регрессионная проверка всех находок |

## Две критичные находки

**P0-1. На закэшированных карточках товара `<title>`, `description`, `canonical` и `og:*` оказываются внутри `<body>`, а не в `<head>`.**

Next.js стримит метаданные браузерам и отдаёт их блокирующим образом ботам — это штатное поведение. Ломается оно на кэше nginx: ключ не учитывает User-Agent, `Vary` его тоже не содержит, поэтому на URL живёт один общий объект. Браузеров кратно больше, чем ботов, так что в кэш попадает стримовый вариант и дальше отдаётся всем.

```
Один URL, оба запроса — промах кэша:
  UA=Twitterbot   title на 2 725 байте    (</head> на 10 381)  ✅
  UA=Chrome       title на 118 623 байте  (</head> на 6 746)   ❌
```

Затрагивает 6 351 карточку — 97 % sitemap. Яндекс, Bing, VK, Telegram и WhatsApp получают именно испорченный вариант.

**P0-2. `?page=N` за пределом диапазона отдаёт 200, пустой список и `index, follow` с self-canonical.**

`?page=999`, `?page=99999`, `?page=1e9` — все 200 и все индексируемые. Параметр не закрыт в robots.txt, умножается на 35 категорий и 19 вариантов `?sub=`. Это генератор soft-404 неограниченного объёма.

## Проверка

```bash
./tools/seo-check.sh
```

Каждая находка оформлена отдельным тестом. Прогон против продакшена 18.08.2026 — **4 PASS, 16 FAIL**; FAIL-ы соответствуют находкам аудита один к одному. По мере выкатки исправлений тесты должны переходить в PASS.

```bash
BASE=https://staging.cosmex.ru ./tools/seo-check.sh   # против стейджа
```

## Что уже сделано хорошо

Сайт технически сильный, и большая часть базы в порядке: HTTPS/HSTS и строгий CSP; все редиректы (`http→https`, `www→apex`, `/catalog/→/catalog`) в один хоп; уникальные title и description (40/40 в выборке); развёрнутая разметка `Organization` / `Store` / `LocalBusiness` / `FAQPage` / `HowTo` / `Service` / `BreadcrumbList` / `ItemList`; `llms.txt` и `llms-full.txt` для AI-краулеров; RSS блога; взаимный hreflang `ru-RU` / `ru-KZ` / `x-default`; sitemap на 6 524 URL без дублей; блог с глубоким контентом и живой перелинковкой в каталог.

Находки ниже — это точечные дефекты на хорошей основе, а не системная запущенность.
